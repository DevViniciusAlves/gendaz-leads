# Ficheiros de ambiente e segredos
.env
.env.*
!.env.example
backend/.env
frontend/.env

# Build / dependencias
node_modules/
dist/
target/
build/
*.class

# Logs
*.log
logs/

# Auth state local do whatsapp-service (contém credenciais Baileys — nunca comitar)
whatsapp-service/data/

# IDE
.idea/
.vscode/
*.iml

# Fontes privadas (adicionar o woff2 real em public/fonts, nunca comitar)
frontend/public/fonts/*.woff2
frontend/public/fonts/*.woff
frontend/public/fonts/*.ttf

# Nested copy do repositorio (evitar gitlink/submodule acidental)
gendaz-leads/
