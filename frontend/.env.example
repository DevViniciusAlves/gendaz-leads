# Backend API URL.
# Empty for local development (uses Vite dev proxy /api -> http://localhost:8080).
# For production, set to the Render backend URL, e.g.:
# VITE_API_URL=https://gendaz-leads.onrender.com
VITE_API_URL=
